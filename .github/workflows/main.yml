name: Publish Commands
on: [push]

env:
  bestFood: potato

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - name: move down
      run: cd ..
    - name: Install modules
      run: npm i
    - name: deploy commands
      run: npm run deploy
